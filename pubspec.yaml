name: flutterblocregistrationapp
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc:

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  flutter_screenutil: ^5.0.0+2
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true


